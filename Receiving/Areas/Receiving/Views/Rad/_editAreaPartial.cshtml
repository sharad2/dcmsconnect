@*@using System.Web.Mvc.Html
@model DcmsMobile.Receiving.ViewModels.Rad.AreaViewModel
@{
    Html.EnableClientValidation();
    Html.EnableUnobtrusiveJavaScript();
    var list = (IEnumerable<SelectListItem>) ViewBag.AreaList;
    var selected = list.FirstOrDefault(p => p.Value == Model.AreaId);
    if (selected != null)
    {
        selected.Selected = true;
    }
}
Select New Area:
@using (Html.BeginForm(FormMethod.Post))
{
    @Html.DropDownListFor(m => m.AreaId, list, "(Not Set)")
    @Html.ValidationSummary()
}

*@

<!--$Id$-->